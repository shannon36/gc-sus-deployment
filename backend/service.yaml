apiVersion: v1
kind: Service
metadata:
  labels:
    app: backend
    name: backendservice
  name: backendservice
  namespace: backend
spec:
  ports:
    - name: http 
      port: 90
      protocol: TCP
    #  targetPort: 9376
    #- name: https
    #  port: 8443
    #  protocol: TCP 
    #  targetPort: 9090
  selector:
    app: backend
  #change from ClusterIP to LoadBalancer for dev purposes
  type: LoadBalancer
